{
  "name": "new-smile-guide-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd new-smile-guide && npm start",
    "build": "cd new-smile-guide && npm install && CI= npm run build",
    "test": "cd new-smile-guide && npm test",
    "netlify": "cd new-smile-guide && npm install && CI= npm run build"
  }
}
